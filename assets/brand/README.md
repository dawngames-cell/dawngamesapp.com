# Owner-supplied Dawn Games brand

`dawn-games-master.jpg` is the unmodified owner attachment received September 18, 2026.

`dawn-games-full.webp` and `dawn-games-dg.webp` are mechanical crops/resizes of that exact image. The logo was not redrawn or approximated. The black background comes from the original JPEG.

`python _tools/brand.py` reproduces the derivative images and root favicon/touch-icon files. Header/footer configuration lives in `_content/site.json`; the OG-image template uses the same supplied full logo.

This authoring note is excluded from the deployment by `_config.yml`.
